// use crate::{combine_paths, get_root_length};

// pub fn get_path_components(path: &str, current_directory: Option<&str>) -> PathComponents {
//     let current_directory = current_directory.unwrap_or(".");
//     let path = combine_paths(current_directory, &[path]);
//     let len = get_root_length(&path);
//     path_components(path, len)
// }
