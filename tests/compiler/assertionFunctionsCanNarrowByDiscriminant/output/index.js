var animal = {
  type: 'cat',
  canMeow: true  
};
assertEqual(animal.type, 'cat');
animal.canMeow;
var animalOrUndef = {
  type: 'cat',
  canMeow: true  
};
assertEqual(animalOrUndef.type, 'cat');
animalOrUndef.canMeow;